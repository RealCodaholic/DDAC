@model IEnumerable<DDAC.Models.Container>

@{
    ViewBag.Title = "Index";
}

<h2>Container</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContainerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalLoad) (Tons)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalLoad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContainerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContainerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContainerID })
        </td>
    </tr>
}

</table>
